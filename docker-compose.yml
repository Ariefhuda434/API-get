services:
  postgres:
    image: postgres:16
    container_name: postgres
    restart: unless-stopped
    environment:
      - POSTGRES_USER=n8n
      - POSTGRES_PASSWORD=n8n_password
      - POSTGRES_DB=n8n
      - TZ=Asia/Jakarta
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U n8n -d n8n"]
      interval: 10s
      timeout: 5s
      retries: 5

  n8n:
    image: n8nio/n8n:latest
    container_name: n8n
    restart: unless-stopped
    ports:
      - "5679:5678"
    environment:
      - TZ=Asia/Jakarta
      - N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS=true
      - N8N_RUNNERS_ENABLED=true
      - N8N_ENCRYPTION_KEY=/7XncDZ0+/11N+w46RQnkDb9D0yDwaep
      - DB_TYPE=postgresdb
      - DB_POSTGRESDB_HOST=postgres
      - DB_POSTGRESDB_PORT=5432
      - DB_POSTGRESDB_DATABASE=n8n
      - DB_POSTGRESDB_USER=n8n
      - DB_POSTGRESDB_PASSWORD=n8n_password
      - N8N_HOST=localhost:5679
      - N8N_PROTOCOL=http
      - N8N_PORT=5678
    volumes:
      - ./n8n/data:/home/node/.n8n
      - ./n8n/data:/files
    depends_on:
      postgres:
        condition: service_healthy

  # viral-cut: (removed - image not available)
  #   image: viral-cut-backup:latest
  #   container_name: viral-cut
  #   restart: unless-stopped
  #   ports:
  #     - "3002:3002"
  #   environment:
  #     - TIKTOK_USERNAME=ksatriawindah
  #     - TIKTOK_PASSWORD=Annisa20061103
  #     - ADMIN_USERNAME=admin
  #     - ADMIN_PASSWORD=admin123
  #     - SESSION_SECRET=viral-cut-session-secret
  #     - KLAP_EMAIL=mr1t6soilax0@web-library.net
  #     - KLAP_API_KEY=kak_dk7hHw0W7WZxjVaFUQ0NHLah
  #     - TZ=Asia/Jakarta

  api-get:
    build: ./api-get
    container_name: api-get
    restart: unless-stopped
    ports:
      - "3003:3002"
    environment:
      - ADMIN_USERNAME=admin
      - ADMIN_PASSWORD=admin123
      - SESSION_SECRET=api-get-session-secret
      - TIKTOK_USERNAME=
      - TIKTOK_PASSWORD=
      - TZ=Asia/Jakarta

volumes:
  postgres-data:
